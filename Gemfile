source 'https://rubygems.org' do
    # Security fixes
    gem 'ffi', '>1.9.24'

    # Sinatra
    gem 'rerun'
    gem 'sinatra'
    gem 'unicorn'
    gem 'foreman'
    gem 'rack-conneg'
    gem 'rack-cache'

    # Graphs
    gem 'gruff'
end
